# A-Maze-ing -- default configuration file.
#
# One KEY=VALUE pair per line.
# Lines starting with '#' and empty lines are ignored.
# Keys are case insensitive, spaces around '=' are trimmed.

# --- Mandatory keys -------------------------------------------------
WIDTH=25
HEIGHT=17
ENTRY=0,0
EXIT=24,16
OUTPUT_FILE=maze.txt

# True  -> perfect maze: exactly one path between entry and exit.
# False -> playable Pac-Man like board: loops, no dead-end.
PERFECT=False

# --- Optional keys --------------------------------------------------

# Seed of the random generator; remove it for a different maze each run.
SEED=42

# backtracker (default), prim or kruskal.
ALGORITHM=backtracker

# Draw the "42" pattern with fully closed cells.
PATTERN=True

# Allow ANSI colours in the terminal rendering.
COLOR=True

# terminal (default) or mlx for a MiniLibX window ("make install-mlx").
# When MiniLibX is unavailable the program falls back to the terminal.
DISPLAY=terminal
